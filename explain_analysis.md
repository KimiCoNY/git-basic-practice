Bản Phân Tích Kế Hoạch Thực Thi (EXPLAIN Analysis)Trước khi tối ưu (Legacy):type: ALL (Full Table Scan)possible_keys / key: NULLrows: ~5,000,000 dòngNguyên nhân: Mệnh đề WHERE YEAR(created_at) = 2026 AND MONTH(created_at) = 6 là truy vấn Non-SARGable. Việc bọc hàm quanh cột buộc MySQL phải quét toàn bộ bảng và tính toán hàm trên từng dòng, chiếm dụng 100% CPU và gây khóa tài nguyên.Sau khi tối ưu (Refactored):type: ref hoặc rangekey: idx_type_daterows: Giảm xuống chỉ còn ước tính vài nghìn dòng phát sinh trong tháng 6.Extra: Using index conditionCơ chế: Khởi tạo Composite Index (transaction_type, created_at) kết hợp chuyển đổi sang điều kiện khoảng created_at >= '2026-06-01' AND created_at < '2026-07-01' giúp biến truy vấn thành SARGable. Database Engine tận dụng cấu trúc B-Tree để nhảy thẳng đến vị trí dữ liệu (Index Seek) và quét trong phạm vi thu hẹp, giải phóng CPU tức thì.Phần 3: Bộ câu hỏi vấn đáp bảo vệ giải pháp với CTOCâu 1: "Thứ tự thực thi thực tế (Execution Order) của các mệnh đề trong SQL là gì? Tại sao WHERE lại chạy trước SELECT?"Trả lời: Thứ tự thực thi logic chuẩn: FROM $\rightarrow$ ON $\rightarrow$ JOIN $\rightarrow$ WHERE $\rightarrow$ GROUP BY $\rightarrow$ HAVING $\rightarrow$ SELECT $\rightarrow$ DISTINCT $\rightarrow$ ORDER BY $\rightarrow$ LIMIT.WHERE phải chạy trước SELECT vì hệ thống cần xác định và thu hẹp tập hợp các dòng thỏa mãn điều kiện lọc từ các bảng nguồn trước khi đưa vào bộ nhớ để cắt tỉa các cột hiển thị, đổi tên (alias) hoặc áp dụng các hàm tổng hợp (SUM, AVG) ở tầng SELECT.Câu 2: "Full Table Scan là gì? Khi nào Full Table Scan lại chạy nhanh hơn Index Scan?"Trả lời: Full Table Scan là hành vi engine đọc tuần tự toàn bộ các khối dữ liệu (pages) của bảng từ đĩa vào RAM.Full Table Scan sẽ vượt trội hơn Index Scan khi:Bảng có kích thước rất nhỏ (vài chục đến vài trăm dòng), chi phí đọc trực tiếp dữ liệu rẻ hơn việc phải đọc qua cấu trúc cây Index rồi trỏ ngược về bảng.Truy vấn cần lấy tỷ lệ dữ liệu quá lớn (ví dụ $> 30\% - 50\%$ hoặc $90\%$ số dòng). Lúc này, việc duyệt cây Index rồi thực hiện tra cứu ngẫu nhiên (Random Disk I/O / Bookmark Lookup) sẽ gây overhead lớn hơn rất nhiều so với đọc tuần tự (Sequential I/O) toàn bảng một lần.Câu 3: "Nếu bảng Transactions bị thao tác INSERT/UPDATE/DELETE hàng nghìn lần/giây, việc tạo thêm nhiều Index sẽ gây rủi ro gì?"Trả lời:Chi phí ghi (Write Amplification): Mỗi khi có một thao tác INSERT, DELETE hoặc UPDATE (trên cột được đánh chỉ mục), MySQL không chỉ ghi dữ liệu vào bảng mà còn phải cập nhật, cân bằng lại tất cả các cây B-Tree Index tương ứng, làm giảm đáng kể thông lượng ghi (Write Throughput).Tăng rủi ro tranh chấp tài nguyên (Lock Contention / Deadlock): Cạnh tranh khóa trên các node của cây chỉ mục dễ gây nghẽn hàng đợi (queue) giao dịch.Phình to dung lượng lưu trữ: Index chiếm dụng dung lượng đĩa và làm loãng bộ nhớ đệm InnoDB Buffer Pool.
